import styles from "../styles/Header.modele.css"

const Header = () => {
  return (
    <div className={styles.contenedor}>
      <h1 className={styles.titulo}>Portofolio</h1>
      <nav className={styles.navegacion}>
      </nav>
    </div>
  );
};

export default Header;
